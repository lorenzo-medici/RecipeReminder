<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.loremed.recipereminder">

    <application
        android:name=".RicettaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.ricette.NewRicettaActivity"
            android:exported="false" />
        <activity
            android:name=".view.lista.ListaActivity"
            android:exported="true" />
        <activity
            android:name=".view.ricette.ExistingRicettaActivity"
            android:exported="false" />
        <activity
            android:name=".view.ricette.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>